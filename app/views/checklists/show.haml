- content_for :title, "My Checklist"

.step
  .step-header.margin-bottom-0
    .step-title.text-center
      %i.icon.icon-large.icon-checklist.white
      %h1 My Checklist
  .row.header-buttons.text-center.margin-bottom-30
    .header-button.columns.small-6
      %a{ href: "", onclick: "window.print()" } PRINT
    .header-button.columns.small-6
      %a{ href: "mailto:?subject=How%20to%20start%20a%20business%20in%20Long%20Beach&body=http://bizport.longbeach.gov/checklist" } EMAIL
  .content-container
    = form_tag(checklist_path, method: 'patch') do
      - ['plan','apply','grow'].each do |step_group|
        - Comfy::Cms::Page.published.find_by(slug: step_group).children.published.order(:position).each do |page, i|
          .row.checklist
            %h3
              %a{ href: page.full_path, style: "text-decoration: underline;" }= page.label
            %ul
              - ChecklistItem.where(category: page.slug.gsub('-', '_')).order(:order_number).each do |item|
                - done = @accomplishment_ids.include? item.id
                = render 'shared/checklist_item', item_id: item.id, item_text: item.item_text, done: done
      = submit_tag "Save My Progress"
    %h3 Need help?
    %p Let us know if you need help completing these tasks by clicking the chat button in the bottom-right corner.
